# Install the engine

Local SF Architect runs a fully-local Python MCP engine. This step installs it
from PyPI using [uv](https://docs.astral.sh/uv/) as an isolated tool:

```
uv tool install sf-local-architect
```

No code leaves your machine. If `uv` is not installed, the extension links you
to the official install guide.
